# traffic-anomaly-detection
🚦 Road traffic anomaly detection pipeline using YOLOv8 + ByteTrack + LSTM Autoencoder + Isolation Forest — detects wrong-way driving, collisions, near-misses, and congestion from video.
